<UserControl x:Class="PackageMagic.WPF.View.PackageItemView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:PackageMagic.WPF.View"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800"
             DataContext="{Binding Source={StaticResource Locator}, Path=Main}">

    <Border BorderThickness="1" BorderBrush="DarkGray">
        <StackPanel Orientation="Vertical" Margin="4">
            <StackPanel Orientation="Horizontal">
                <Label Content="Id:" Width="80"/>
                <TextBox Text="{Binding SelectedPackage.Id}" Width="200"/>
            </StackPanel>
            <StackPanel Orientation="Horizontal">
                <Label Content="Path:" Width="80"/>
                <TextBox Text="{Binding SelectedPackage.Path}" Width="200"/>
            </StackPanel>
            <StackPanel Orientation="Horizontal">
                <Label Content="Type:" Width="80"/>
                <TextBox Text="{Binding SelectedPackage.Type}" Width="200"/>
            </StackPanel>
        </StackPanel>
    </Border>
</UserControl>
